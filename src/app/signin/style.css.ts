import { style } from "@vanilla-extract/css";

export const signin = style({
  minHeight: "100vh",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});

export const continueWithGoogle = style({
  width: "60%",
});
